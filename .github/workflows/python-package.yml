#此工作流程将安装Python依赖项，运行各种Python版本的测试和lint
#有关更多信息，请参见:https://docs . github . com/en/actions/automating-build-and-tests/build-and-testing-python

名字：名称:Python包

在：
推：
分支：[分支：【“主“】"主要" ]
拉取请求：
分支：[分支：【“主“】"主要" ]

工作：
建设：

    runs-on：运行：Ubuntu-最新版
    strategy：
      fail-fast: false
      matrix:
        python-version: ["3.9",python版本:【“3.9”、“3.10”、“3.11”】"3.10", "3.11"]

    steps:
    - uses: -用途:actions/checkout@v3
    - name: - name:设置Python $ { { matrix . Python-version } }{{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install flake8 pytest
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Test with pytest
      run: |
        pytest
